# GalaxySSI Embedded Local Model

Full offline IPA builds stage the verified LFM2.5 350M Q8_0 GGUF model in this folder. On first use, GalaxySSI verifies the model digest and copies it into its private Application Support model store before native llama inference loads it.
